services:
  animesub:
    image: ghcr.io/hhlyyng/animesub:latest
    restart: unless-stopped
    ports:
      - "5072:5072"    # change the left port to your preferred host port
    volumes:
      - ./config:/app/data    # persistent data: database, runtime config
